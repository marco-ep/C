#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
	double a,b,c,x;

	printf("enter values for a, b, and c");
	scanf("%lf %lf %lf", &a, &b, &c);

	if (a=b=0) {
		printf("no solution\n");
	}else if(a=b=c=0){
		printf("anything works\n");
	}




	a*sqrt(x)+b*x+c=0